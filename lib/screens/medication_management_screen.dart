import 'package:flutter/material.dart';

class MedicationManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Medication Management'),
      ),
      body: Center(
        child: Text('Medication Management Screen'),
      ),
    );
  }
}
