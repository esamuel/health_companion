// health_education_screen.dart
import 'package:flutter/material.dart';

class HealthEducationScreen extends StatelessWidget {
  const HealthEducationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Health Education')),
      body: Center(child: Text('Health Education Resources Coming Soon')),
    );
  }
}

