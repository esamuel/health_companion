specVersion: "v1beta"
serviceId: "health_companion"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "health_companion-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
